<%= @attraction.name %>
<%= @attraction.tickets %>

<% if @user.admin != true %>
  <%= button_to "Go on this ride", rides_path, params: {id: @attraction.id} %>
<% else %>
  <%= link_to "Edit Attraction", edit_attraction_path %>
<% end %>
